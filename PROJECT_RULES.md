# TTLAB 项目开发规则

## 1. 总则

TTLAB 必须按照正式生产系统进行设计、开发、测试和交付。

禁止采用 MVP、临时方案、演示方案、一次性脚本或只验证主流程的实现方式。任何功能都必须从正式架构、数据模型、异常处理、安全控制、测试和部署角度完整设计后再实现。

禁止使用以下方式代替正式实现：

- 使用静态数据或假接口冒充真实业务能力。
- 只实现成功路径，忽略失败、超时、断线、权限不足和设备离线。
- 将核心逻辑全部写在单个文件或单个函数中。
- 使用未定义格式的临时消息或隐式约定进行 Server/Client 通信。
- 通过手工修改数据库、配置文件或运行环境解决正常业务问题。
- 以“后续再补”为理由省略安全、日志、测试、监控和部署能力。

## 2. 系统边界

系统必须保持以下职责边界：

```text
Web 控制台：用户交互和数据展示
Server：认证、授权、设备管理、指令调度、状态同步和审计
Client：Linux 设备接入、串口发现、串口操作和本地执行
串口适配层：设备协议和具体串口读写实现
```

- Web 不得直接访问串口设备。
- Server 不得依赖本地串口环境执行设备操作。
- Client 不得绕过 Server 接受未经授权的远程指令。
- 串口协议实现必须与 Client 通信、业务调度逻辑解耦。
- 每个模块必须有明确输入、输出、错误格式和生命周期。

## 3. 方案设计要求

任何功能开始编码前，必须完成对应设计，至少包括：

- 业务目标和使用场景。
- 模块职责和依赖关系。
- 数据模型和数据生命周期。
- API 或通信协议定义。
- 权限模型和安全边界。
- 正常、异常、超时、重试和恢复流程。
- 配置项、默认值和环境差异。
- 日志、指标、告警和审计要求。
- 测试策略和验收标准。
- 部署、升级、回滚和兼容性方案。

设计文档必须与实际实现保持一致。实现发生变化时，必须同步更新设计文档、接口文档和运维文档。

## 4. Server 规则

Server 必须具备正式服务所需的完整能力：

- 用户认证和会话管理。
- 基于角色和资源的权限控制。
- Client 注册、身份校验和生命周期管理。
- Client 心跳、在线状态和断线检测。
- 串口设备注册、发现、变更和状态管理。
- 指令创建、排队、分发、执行、超时和结果记录。
- 请求唯一 ID、幂等控制和重复指令防护。
- 统一错误码和错误响应格式。
- 结构化日志、运行指标和审计记录。
- 数据库迁移、备份、恢复和版本管理。
- 健康检查、优雅关闭和异常恢复。

Server 不得把设备状态只保存在内存中，也不得依赖重启后自动恢复的临时状态作为正式数据。

## 5. Client 规则

Linux Client 必须作为正式后台服务交付：

- 使用唯一身份连接 Server。
- 支持安全注册、心跳和自动重连。
- 支持网络中断、Server 重启和 Client 重启后的状态恢复。
- 支持串口设备首次发现、热插拔和设备移除。
- 支持串口打开失败、读写失败、设备占用和设备断开的处理。
- 同一串口必须具备并发访问保护。
- 所有操作必须有超时、取消和结果回传机制。
- Client 异常退出后必须能够被 `systemd` 自动拉起。
- Client 配置、日志和运行目录必须符合 Linux 部署规范。
- 不得在 Client 中硬编码 Server 地址、密钥或设备规则。

串口设备的身份不能只依赖 `/dev/ttyUSB0` 等可能变化的设备路径，必须结合稳定的硬件信息或明确的设备标识进行识别。

## 6. 通信协议规则

Server 与 Client 的通信协议必须版本化、可校验、可追踪：

- 每条消息必须包含消息 ID、消息类型、时间戳和协议版本。
- 请求和响应必须能够明确关联。
- 消息必须定义成功、失败、超时和未知类型的处理方式。
- 消息结构必须使用明确的 Schema 或类型定义。
- 新增字段必须保持向后兼容。
- 不允许通过字符串拼接或未文档化字段传递业务数据。
- 指令必须具备幂等策略，避免重试导致重复执行。
- 大量日志或数据必须考虑大小限制、分片或流式传输。
- 生产环境必须使用 TLS/WSS 或等效的安全通信方式。

建议统一使用以下消息信封结构：

```json
{
  "id": "req_000001",
  "version": "1.0",
  "type": "command.execute",
  "timestamp": "2026-08-28T10:00:00Z",
  "clientId": "client-001",
  "payload": {}
}
```

## 7. 数据和状态规则

设备、Client、指令和事件必须建立持久化数据模型，至少区分：

- 注册状态。
- 在线状态。
- 设备连接状态。
- 指令排队状态。
- 指令执行状态。
- 操作成功或失败状态。
- 事件发生时间和接收时间。

状态变化必须有明确的状态机，不得通过多个布尔字段组合出未定义状态。

所有时间统一使用 UTC 存储，展示层根据用户或系统时区转换。

删除设备、Client 或操作记录时必须考虑审计、关联数据和恢复策略，不得直接物理删除重要审计数据。

## 8. Web 控制台规则

Web 控制台必须连接真实 Server 接口，不得将静态原型作为最终实现。

所有主要页面和操作必须完整处理以下状态：

- 初始加载。
- 加载中。
- 空数据。
- 正常数据。
- 权限不足。
- 网络断开。
- Server 错误。
- Client 离线。
- 设备不可用。
- 指令执行中、成功和失败。

界面必须保持清晰的信息层级，避免无意义的装饰、重复数据和无法操作的假控件。按钮、表格、表单、弹窗和通知必须具有真实行为、键盘可操作性和明确反馈。

## 9. 安全规则

- 不得将密钥、Token、密码和私钥提交到仓库。
- 所有用户输入和设备指令必须进行校验。
- 所有设备操作必须进行权限检查。
- 高风险操作必须支持二次确认或审批策略。
- Client 身份必须可撤销和轮换。
- 生产环境禁止使用默认密码和通用 Token。
- 日志中不得输出密码、完整 Token 或敏感设备数据。
- 重要操作必须记录操作者、目标设备、指令、结果和时间。
- 依赖项必须定期检查安全漏洞和版本兼容性。

## 10. 测试规则

正式功能必须同时具备：

- 单元测试：验证业务逻辑、状态机和协议解析。
- 集成测试：验证 Server、数据库和通信层协作。
- Client 测试：验证串口发现、断线重连和指令执行。
- 端到端测试：验证 Web 到 Server 再到 Client 的完整流程。
- 异常测试：验证超时、断网、设备拔出、重复请求和权限失败。
- 浏览器测试：验证桌面端、移动端、交互和控制台错误。

真实串口设备不可用时，必须使用可控的 Fake Serial Adapter 或虚拟串口进行测试，不得跳过测试。

## 11. 部署和运维规则

正式部署必须提供：

- 开发、测试和生产环境配置区分。
- 可重复执行的构建和部署流程。
- 数据库迁移脚本。
- Server 健康检查和优雅关闭。
- Client `systemd` 服务文件。
- 日志轮转和保留策略。
- 配置项说明和密钥注入方式。
- 版本号、发布记录和回滚方案。
- 数据库备份和恢复说明。
- 故障排查文档。

任何部署步骤不得依赖开发人员本机的临时状态或手工操作。

## 12. 代码质量规则

- 优先使用清晰、可维护和可测试的实现。
- 模块、函数和变量命名必须表达真实职责。
- 避免超长函数、重复逻辑和隐式全局状态。
- 核心逻辑必须有必要的注释和类型约束。
- 不得提交调试输出、临时代码、未使用依赖和死代码。
- 不得为了快速完成而关闭类型检查、Lint 或测试。
- 修改公共接口时必须同步更新调用方和文档。
- 代码变更必须保持向后兼容，或提供明确的迁移步骤。

## 12.1 开发工作区纪律

所有开发、修改、重构和代码评审必须在本仓库的独立 git worktree 中完成，禁止直接在共享的主工作区（仓库根目录）进行任何代码修改。

- 每个开发任务（功能、修复、重构）开始前，必须先创建独立的 worktree，并在该 worktree 内完成全部开发、构建、测试和提交。
- 共享的主工作区只用于审查已提交的代码、执行发布构建和查看仓库状态，不得在其中编辑代码或产生未提交的本地改动。
- worktree 命名必须能表达任务内容，例如 `git worktree add ../ttlab-feature-xxx main`；任务完成后按仓库约定合并回主分支，再清理该 worktree。
- 任何人在主工作区或他人 worktree 中发现未提交改动，必须先确认来源；不得擅自丢弃、覆盖或提交不属于当前任务的改动。
- 违反本条纪律视为开发流程违规，相关改动不予评审和合并。

## 13. 交付标准

功能只有同时满足以下条件才算完成：

- 设计文档已完成并与实现一致。
- Server、Client、Web 的完整链路可运行。
- 正常路径和异常路径均已实现。
- 权限、日志、审计和错误处理已覆盖。
- 单元、集成和端到端测试已通过。
- 浏览器端无控制台错误和明显布局问题。
- 部署、配置、升级和回滚文档已完成。
- 代码已通过评审，仓库状态干净。
- 没有遗留的假数据、占位实现或“后续补充”核心功能。

任何不满足以上标准的实现只能视为未完成，不得以演示可用或主流程可用作为交付依据。
